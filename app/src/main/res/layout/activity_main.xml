<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--
        android:id => Set ID
        android:layout_width và android:layout_height => Set chiều cao và chiều rộng
        android:padding => Khoảng cách từ content đến viền
        android:text => Định nghĩa văn bản được hiển thị bởi một widget TextView
        android:textAlignment => căn lề văn bản được hiển thị bởi một widget TextView
        android:textColor => Set màu chữ
        android:textSize => Set cỡ chữ
        android:textStyle => Set kiểu chữ: in đậm, in nghiêng, bình thường...

        Ở TextView (id là idTVStatus)
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            => Nằm ở giữ màn hình theo cả chiều ngang và dọc

        Ở Switch
            android:layout_marginTop="60dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            => Nằm giữa theo chiều ngang và nằm dưới TextView 60dp
    -->

    <TextView
        android:id="@+id/idTVStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text="Status"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="MissingConstraints" />

    <Switch
        android:id="@+id/idSwitch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/idTVStatus"
        tools:ignore="MissingConstraints,UseSwitchCompatOrMaterialXml" />

    <!--
    tools:ignore="MissingConstraints"   => bỏ qua lỗi "Missing Constraints"

    MissingConstraints: Lỗi này xảy ra khi một widget không được ràng buộc với bất kỳ đối tượng nào khác.
    UseSwitchCompatOrMaterialXml: Lỗi này xảy ra khi bạn sử dụng widget Switch trong một layout XML mà không sử dụng namespace androidx.appcompat.
    -->

</androidx.constraintlayout.widget.ConstraintLayout>
